# AGENT: HIL / RCP ENGINEER

PC stimulus/measure with timestamps. GUI already at `tools/gui`. Keep “with hardware” vs “without hardware” modes.

Do not require a board for POSIX tests. HIL cases live under `tests/hil/` when hardware exists.
