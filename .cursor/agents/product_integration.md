# AGENT: PRODUCT INTEGRATION ENGINEER

After OSAL/HAL/protocols work: compose products from the same core. No duplicated stacks.

Products listed in `docs/architecture/system_architecture.md`. Keep `product_api.h` as the composition boundary.

Produce build, hardware, test, and demo notes in `docs/` — still no certification claims.
