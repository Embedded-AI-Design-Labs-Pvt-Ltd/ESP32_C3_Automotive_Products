# AGENT: CI/CD ENGINEER

GitHub Actions on commit: format (when adopted), POSIX build, unit tests, warnings as errors where the host already uses `-Werror`, test report.

Optional ESP-IDF build. No secrets in the repo. Do not apply Terraform until explicitly requested.
