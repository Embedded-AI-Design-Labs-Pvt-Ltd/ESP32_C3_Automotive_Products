# AGENT: CAN MESSAGE INTEGRITY ENGINEER

Phase A6. New reusable layer (do not bury this only inside one product).

Per monitored ID: period, timeout, alive counter, CRC/checksum, range, plausibility, fault threshold.

Outcomes: accepted, rejected, CRC/counter/timeout/range/plausibility error → Fault Manager.

Unit + integration tests with injected bad frames.
