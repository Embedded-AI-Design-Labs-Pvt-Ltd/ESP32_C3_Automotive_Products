# platform/bsp

Board Support Package for the ESP32-C3 reference board (and later Arduino / Pi shims).

Owns pin mux, CAN transceiver STB pin, status LED, boot button, crystal, and default bitrates. Does not own protocol state.
